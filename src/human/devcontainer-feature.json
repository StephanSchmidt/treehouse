{
  "id": "human",
  "version": "1.2.0",
  "name": "human CLI",
  "description": "Installs the human CLI — issue tracker interface for AI agents",
  "documentationURL": "https://github.com/StephanSchmidt/treehouse",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version of the human CLI to install (e.g. '0.4.0' or 'latest')"
    },
    "proxy": {
      "type": "boolean",
      "default": false,
      "description": "Install HTTPS proxy support (iptables + setup script). Set HUMAN_PROXY_ADDR env var and run human-proxy-setup at container start. Requires NET_ADMIN capability."
    }
  },
  "containerEnv": {
    "BROWSER": "human-browser"
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
